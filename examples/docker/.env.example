# USG RADIUS Docker Environment Variables
# Copy this file to .env and fill in your secrets

# IMPORTANT: Never commit the actual .env file to version control!

# ============================================================================
# Core RADIUS Configuration
# ============================================================================

# Default shared secret (REQUIRED)
# Use a strong random value (16+ characters)
RADIUS_SECRET=CHANGE_ME_TO_STRONG_RANDOM_SECRET

# ============================================================================
# Client Configuration
# ============================================================================

# Client 1 (e.g., Wireless Access Points)
CLIENT_1_NETWORK=192.168.1.0/24
CLIENT_1_SECRET=CHANGE_ME_CLIENT_1_SECRET
CLIENT_1_NAME=Wireless Access Points

# Client 2 (e.g., VPN Gateway)
CLIENT_2_NETWORK=10.0.0.1
CLIENT_2_SECRET=CHANGE_ME_CLIENT_2_SECRET
CLIENT_2_NAME=VPN Gateway

# ============================================================================
# User Configuration
# ============================================================================

# Admin credentials (REQUIRED)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_ME_ADMIN_PASSWORD

# ============================================================================
# Logging Configuration
# ============================================================================

# Log level: trace, debug, info, warn, error
LOG_LEVEL=info

# Rust logging (can be more specific, e.g., "usg_radius=debug,radius_server=trace")
RUST_LOG=info

# ============================================================================
# Generating Strong Secrets
# ============================================================================

# Linux/macOS:
#   openssl rand -base64 32
#   head -c 32 /dev/urandom | base64

# Or use a password manager to generate strong random secrets

# ============================================================================
# Security Best Practices
# ============================================================================

# 1. Use unique secrets for each client
# 2. Use secrets with 16+ characters including uppercase, lowercase, numbers, symbols
# 3. Never commit secrets to version control
# 4. Add .env to .gitignore
# 5. Rotate secrets regularly (quarterly recommended)
# 6. Use Docker secrets or Kubernetes secrets for production deployments
